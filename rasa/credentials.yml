# Configuración de credenciales para canales de Rasa

rest:
  # Habilitar el canal REST
  enabled: true

# Configuración para el canal de Telegram
#telegram:
#  access_token: ${TELEGRAM_TOKEN}
#  verify: ${TELEGRAM_BOT_NAME}
#  webhook_url: ${WEBHOOK_URL}/webhooks/telegram/webhook

# Configuración para el canal de Facebook Messenger
#facebook:
#  verify: ${FB_VERIFY}
#  secret: ${FB_SECRET}
#  page-access-token: ${FB_PAGE_TOKEN}

# Configuración para el canal de WhatsApp (Twilio)
#twilio:
#  account_sid: ${TWILIO_ACCOUNT_SID}
#  auth_token: ${TWILIO_AUTH_TOKEN}
#  twilio_number: ${TWILIO_NUMBER}
