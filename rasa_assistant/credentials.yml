# This file contains the credentials for the voice & chat platforms
# which your bot is using.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

rest:
  # Habilitar la API REST para que tu aplicación pueda comunicarse con Rasa
  cors_origins: ["*"]  # En producción, especifica los dominios exactos

# Puedes añadir más canales según sea necesario
